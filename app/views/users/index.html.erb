<h1>Users</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      id: <%= user.id %>, name: <%= link_to user.name, user_path(user) %>
      | <%= link_to "View Posts", user_posts_path(user) %>
    </li>
  <% end %>
</ul>
