<h1><%= @user.name %>'s Posts</h1>
<ul>
  <% @posts.each do |post| %>
    <li>
      Post ID: <%= post.id %>, Title: <%= link_to post.title, user_post_path(@user, post) %>
    </li>
  <% end %>
</ul>
